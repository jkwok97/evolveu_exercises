function sum(a, b) {
    return a + b;
  }
//   module.exports = sum;

function add(a, b) {
    return a + b + 1;
}

class LinkList {

    play(str) {
        console.log(str);
        return str
    }
  }

export default { sum, add, LinkList };